name: CI/CD Pipeline

on:
  push:
    branches:
      - debug-loop-logging-fix
      - fix-cloud-init-version
  pull_request:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Clear pip cache
      run: |
        pip cache purge

    - name: Clear GitHub Actions cache
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libsystemd-dev meson

    - name: Install Cython
      run: |
        python -m pip install --upgrade pip
        pip install cython

    - name: Install dependencies
      run: |
        pip install --no-build-isolation -r requirements.txt --verbose || true

    - name: Print flake8 configuration
      run: |
        cat .flake8

    - name: Print last commit hash
      run: |
        git log -1 --pretty=format:"%H"

    - name: Lint with flake8
      run: |
        pip install flake8
        flake8 --version
        flake8 --bug-report
        flake8 --config .flake8 .

    - name: Run tests with pytest
      run: |
        pip install pytest
        pytest
